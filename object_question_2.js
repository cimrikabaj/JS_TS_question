// Program that takes an object and returns the number of properties the object has.

const person1 = {
    name: "ram",
    age: 25,
    address: "nepal"
  };

  console.log(Object.keys(person1).length);